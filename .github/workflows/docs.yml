name: Docs

# Checks the documentation site with the Blume CLI. Deployment is handled by
# Cloudflare Workers Builds, not by this workflow.

on:
  push:
    branches: [main]
    paths:
      - 'apps/docs/**'
      - 'packages/**'
      - 'package.json'
      - 'bun.lock'
      - '.github/workflows/docs.yml'
  pull_request:
    paths:
      - 'apps/docs/**'
      - 'packages/**'
      - 'package.json'
      - 'bun.lock'
      - '.github/workflows/docs.yml'

permissions:
  contents: read

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      # The docs import the workspace packages, so build them first.
      - run: bun run build:packages
      - name: Type-check
        run: bun run --cwd apps/docs check
      - name: Diagnose config and content
        run: bun run --cwd apps/docs doctor
      - name: Validate links
        run: bun run --cwd apps/docs validate
      # Fails when an English page changed after its translation was recorded
      # in blume.translations.json. Rerun `blume translate` and commit the result.
      - name: Check translations are up to date
        run: bun run --cwd apps/docs translate:check
      - name: Build
        run: bun run --cwd apps/docs build
